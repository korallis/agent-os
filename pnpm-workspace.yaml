packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks lifecycle scripts by default; these are the only
# dependencies allowed to run their (native-binary) install scripts.
onlyBuiltDependencies:
  - better-sqlite3
  - sharp
  - unrs-resolver
